Below is the complete package for the Git clone task: **OTS, runbook, and Mermaid**.

## OTS
Run this on the **Storage Server** as **natasha**.

```bash
#!/bin/bash
set -e

cd /usr/src/kodekloudrepos
git clone /opt/demo.git
```

## Runbook
1. SSH into the Storage Server as `natasha`.
2. Verify the target directory exists:
```bash
ls -ld /usr/src/kodekloudrepos
```
3. Change into the target directory:
```bash
cd /usr/src/kodekloudrepos
```
4. Clone the repository:
```bash
git clone /opt/demo.git
```
5. Verify the clone:
```bash
ls -l /usr/src/kodekloudrepos
```

## Mermaid
```mermaid
flowchart LR
    A[Login as natasha on Storage Server]:::start --> B[cd /usr/src/kodekloudrepos]:::dir
    B --> C[git clone /opt/demo.git]:::clone
    C --> D[Verify cloned repo exists]:::verify
    D --> E[Task complete]:::done

    classDef start fill:#fff3cd,stroke:#f1c40f,stroke-width:2px,color:#000;
    classDef dir fill:#e6f7ff,stroke:#3498db,stroke-width:2px,color:#000;
    classDef clone fill:#fce4ec,stroke:#d81b60,stroke-width:2px,color:#000;
    classDef verify fill:#e8f8e8,stroke:#27ae60,stroke-width:2px,color:#000;
    classDef done fill:#d5f5e3,stroke:#2ecc71,stroke-width:2px,color:#000;
```

If you want, I can also add **pre-checks and post-checks** in the same format.