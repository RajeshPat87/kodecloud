Selector is `app: nginx-app`. Now apply all three changes:

## Step 1 — Update deployment (replicas 1→5, image nginx:1.18→nginx:latest)

```bash
kubectl get deployment nginx-deployment -o yaml \
  | sed 's|replicas: 1|replicas: 5|g' \
  | sed 's|image: nginx:1.18|image: nginx:latest|g' \
  | kubectl apply -f -
```

## Step 2 — Update service (nodeport 30008→32165)

```bash
kubectl get service nginx-service -o yaml \
  | sed 's|nodePort: 30008|nodePort: 32165|g' \
  | kubectl apply -f -
```

## Step 3 — Monitor rollout

```bash
kubectl rollout status deployment/nginx-deployment
```

## Step 4 — Verify all three changes

```bash
# replicas: expect 5/5
kubectl get deployment nginx-deployment

# image: expect nginx:latest
kubectl describe deployment nginx-deployment | grep Image

# nodeport: expect 32165
kubectl get service nginx-service
```

Expected:
```
NAME               READY   UP-TO-DATE   AVAILABLE
nginx-deployment   5/5     5            5

Image: nginx:latest

NAME            TYPE       CLUSTER-IP      PORT(S)
nginx-service   NodePort   10.43.233.243   80:32165/TCP
```

All three changes applied without deleting the deployment or service. The selector `app: nginx-app` is preserved in the existing manifest pulled by `get -o yaml`, so no immutability error occurs.

Selector is `app: nginx-app`. Now apply all three changes:

## Step 1 — Update deployment (replicas 1→5, image nginx:1.18→nginx:latest)

```bash
kubectl get deployment nginx-deployment -o yaml \
  | sed 's|replicas: 1|replicas: 5|g' \
  | sed 's|image: nginx:1.18|image: nginx:latest|g' \
  | kubectl apply -f -
```

## Step 2 — Update service (nodeport 30008→32165)

```bash
kubectl get service nginx-service -o yaml \
  | sed 's|nodePort: 30008|nodePort: 32165|g' \
  | kubectl apply -f -
```

## Step 3 — Monitor rollout

```bash
kubectl rollout status deployment/nginx-deployment
```

## Step 4 — Verify all three changes

```bash
# replicas: expect 5/5
kubectl get deployment nginx-deployment

# image: expect nginx:latest
kubectl describe deployment nginx-deployment | grep Image

# nodeport: expect 32165
kubectl get service nginx-service
```

Expected:
```
NAME               READY   UP-TO-DATE   AVAILABLE
nginx-deployment   5/5     5            5

Image: nginx:latest

NAME            TYPE       CLUSTER-IP      PORT(S)
nginx-service   NodePort   10.43.233.243   80:32165/TCP
```

All three changes applied without deleting the deployment or service. The selector `app: nginx-app` is preserved in the existing manifest pulled by `get -o yaml`, so no immutability error occurs.