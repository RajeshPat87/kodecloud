## TODO

- [ ] Create `main.tf` in `/home/bob/terraform/`
- [ ] Define `aws_vpc` resource named `xfusion-vpc` with any IPv4 CIDR
- [ ] Add `Name` tag = `xfusion-vpc` (how AWS names the VPC)
- [ ] Run `terraform init` → `terraform apply -auto-approve`
- [ ] Verify via `terraform state show`

## main.tf content

Paste into `/home/bob/terraform/main.tf`:

```hcl
resource "aws_vpc" "xfusion_vpc" {
  cidr_block = "10.0.0.0/16"

  tags = {
    Name = "xfusion-vpc"
  }
}
```

## Apply

```bash
cd /home/bob/terraform
terraform init
terraform apply -auto-approve
```

Expected:
```
aws_vpc.xfusion_vpc: Creation complete... [id=vpc-...]
Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
```

## Verify

```bash
terraform state show aws_vpc.xfusion_vpc
```

Expected:
```
cidr_block = "10.0.0.0/16"
tags = {
  "Name" = "xfusion-vpc"
}
```

## Requirement trace

| Requirement | Evidence |
|---|---|
| VPC named `xfusion-vpc` | `tags = { Name = "xfusion-vpc" }` |
| Any IPv4 CIDR | `cidr_block = "10.0.0.0/16"` |
| Region `us-east-1` | `provider.tf` |
| `main.tf` only | single file, one resource |